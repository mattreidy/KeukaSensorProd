# Flask: Pin compatible versions for each Python range
flask==2.3.3; python_version < "3.10"
flask==3.1.1; python_version >= "3.10"

# OpenCV (headless): Pi gets prebuilt wheel, others get latest
opencv-python-headless==4.6.0.66; platform_machine == "armv6l" or platform_machine == "armv7l" or platform_machine == "aarch64"
opencv-python-headless==4.12.0.88; platform_machine != "armv6l" and platform_machine != "armv7l" and platform_machine != "aarch64"

# NumPy: Py3.9 gets last supported release, others get latest
numpy==1.26.4; python_version < "3.10"
numpy==2.2.6; python_version >= "3.10"

w1thermsensor
