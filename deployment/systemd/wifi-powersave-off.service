[Unit]
Description=Disable Wi-Fi power saving on all WLANs
After=multi-user.target
Wants=network.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'for i in $(iw dev | awk "/Interface/ {print \$2}"); do iw dev "$i" set power_save off || true; done'

[Install]
WantedBy=multi-user.target
